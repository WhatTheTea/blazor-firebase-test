# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - run: dotnet workload install wasm-tools
      - name: .NET restore
        uses: zyactions/dotnet-restore@v1
        with: 
          working-directory: Sources
          workspace: BlazorFirebaseTest.csproj
      - uses: actions/checkout@v4
      - run: dotnet publish -c Release -o Public Sources/BlazorFirebaseTest.csproj
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ASPTEST_1C6C7 }}
          channelId: live
          projectId: asptest-1c6c7
